Persona:
you are a mcp (model context protocol) maker, who already has a lot of experience in making mcp, and you are very good at it. you have made many mcp before, and you know how to make a good mcp. you are also very creative, and you can come up with new ideas for mcp. you are also very patient, and you can take your time to make a good mcp.

Context:
currently we manually identify and fetch which code that not deploy to production yet, and we have to manually find which pr that already merged to master but still not triggering any production pipeline, which is very time consuming. we want to automate this process by creating a mcp that can automatically identify and fetch the code changes that have been merged to master but still not deployed to production, and give us a reminder on which code changes that we need to deploy to production.

for the context we are using trunk based development, so all the code changes will be merged to master branch, and then we have a pipeline that will automatically deploy the code changes to production. however, sometimes there are some code changes that have been merged to master but still not deployed to production, either because of some issues in the pipeline or some other reasons. this mcp will help us identify and fetch those code changes, and give us a reminder on which code changes that we need to deploy to production.

Task:
i need you to plan and implement this mcp, here is the tools that we needs:
- can automatically identify and fetch the code changes that have been merged to master but still not deployed (not triggering pipeline production) to production, and give us a reminder on which jira ticket or code changes that we need to deploy to production. The reminder should be sending the list of jira ticket, including the pr ticket, the author, and the summary of the changes, to a google chat group, so that the team can be aware of which code changes that need to be deployed to production, and can take action accordingly. this will help us improve our communication and collaboration within the team, and ensure that we are all on the same page regarding the code changes that need to be deployed to production. you can extract the jira ticket either on the name of the branch on the pr, pr title, pr description or the commit message, but if you couldnt find any jira ticket you can fill the blank the jira ticket column with "unknown ticket".

the requirement of the tickets or task that still not deployed to production is, checking where the latest tag was created on the commits master, and see the commit after the last commit and tag created. theres the list tasks or tickets that still not deployed to production.

- can help us creating new tag from latest tag was created, by creating tag based on latest commit on the master branch, and the tag name should follow existing tag pattern on each branch. everytime tag was successfully created, the mcp should also send a notification to the google chat group, so that the team can be aware of the new tag and can take action accordingly. this will help us improve our communication and collaboration within the team, and ensure that we are all on the same page regarding the new tags that are being created.

- creating a deployment document on the confluence by following template that we already define. the template or the outline for each deployment document should be configurable, but the easier way to do it is by cloning on existing and latest document deployment and make little adjustment on the content. the deployment document also need to configurable by attaching old deployment document as reference, so that we can easily clone the content and make adjustment on the new deployment document. this will help us save time and effort in creating the deployment document, and ensure that we are providing all the necessary information for the deployment process.

- triggering run pipeline production or non-production environment, the pipeline name should be configurable for each repository, so that we can specify which pipeline we want to trigger for each repository, and the mcp can automatically trigger the pipeline based on the configuration. this will help us ensure that we are triggering the correct pipeline for each repository, and that we are following the correct deployment process for each repository. for non-live environment pipeline should be using selected long commit hash or by default choosing latest commit hash on the master, but for live it should be using the latest tag name just created. when this tools being execute, mcp should know which latest tag for the production and which latest commit for the non-production.


Notes:
- repository list that included to check the code changes that still not deployed to production should be configurable, so we can specify which repositories we want to check for the code changes that still not deployed to production. for the configuration reference you can see others implementation config mcp at mcp workspace. adjust with our needs and requirements, and make sure that the configuration is easy to use and understand for users.
- make sure you doing the planning and produce plan.md first before doing any implementation.
- make sure you are write using go language, so i can easily read and debug how we implement this tools.
- make sure you when writing go, you follow the best practices and conventions for go programming, and make sure that the code is clean, readable, and maintainable. you can also include comments and documentation in the code to explain the logic and functionality of the code, and to help other developers understand and use the code effectively.
- make sure well seperation of concerns in the code, and make sure that each function and module has a clear and specific responsibility, and that the code is organized in a way that makes it easy to understand and maintain. you can also consider using design patterns and principles to help structure the code and improve its maintainability and scalability.